"""Supervised event-type classification.

Trains SVM (linear and RBF), Decision Tree, and Gaussian Naive Bayes on the
DBSCAN-derived labels with stratified k-fold cross-validation, plus a KNN
magnitude-bin estimator. Uses LDA for 2D projection plots.

See PROJECT_PROPOSAL.md section 7 (Phases E-G).
"""


def main() -> None:
    raise NotImplementedError("Weeks 8-10 deliverable")


if __name__ == "__main__":
    main()
